#include <stdio.h>
#include <string.h>
/*
void swap(int *p, int *q) {
    int t;
    t = *p;
    *p = *q;
    *q = t;
}
*/

/*
void search_all_occurrences(char *str,char ch,int *arr){
    int j = 0;
    for(int i = 0; str[i]; i++){
        if(str[i] == ch){
            arr[j] = i;
            j++;
        }
    }
    arr[j] = 0;
}
    */

/*char* uppercase(char* str){
    for(int i = 0; str[i]; i++){
        if(str[i] >= 'a' && str[i] <= 'z'){
            str[i] = str[i] - 32;
        }
    }
    return str;
} */

/*
char* lowercase(char* str){
    for(int i = 0; str[i]; i++){
        if(str[i] >= 'A' && str[i] <= 'Z'){
            str[i] = str[i] + 32;
        }
    }
    return str;
}
*/

void extract_string(char* str, int start, int end, char* ans){
    int i;
    for(i  = 0 ; i < end; i++){
        ans[i] = str[i+start];
    }
    ans[i] = 0;
}

int main(){
    char str[] = "Mohitlohiya";
    int size = strlen(str);
    char ans[size];
    extract_string(str,1,5,ans);
    printf("%s\n",ans);
    return 0;
}
